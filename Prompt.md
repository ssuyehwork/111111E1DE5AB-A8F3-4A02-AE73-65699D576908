这里是为你准备的全套「开箱即用」强制约束提示词（Prompt）。你只需要按照顺序，将这些提示词直接复制并发送给 Jules 即可。

提示词一：建立开发契约（首次对话时发送）

使用时机：在一个全新的对话窗口中，将这段话连同**《ArcMeta 开发需求文档 (终极版)》全文**一起发送给它。

code
Text
download
content_copy
expand_less
你现在是 ArcMeta 项目的首席 Windows C++ 架构师。请仔细阅读我随后附上的《ArcMeta 开发需求文档》。

【强制约束指令】：
1. 收到此文档后，此时绝对不要生成任何业务代码！
2. 请向我明确确认你已经深刻理解了文档中的【五步闭环流程（理解、确认、执行、自检、汇报）】以及所有的【全局开发红线】。
3. 请用你自己的话，向我复述你对【第一阶段（核心数据层）】中准备优先开发的第一个子模块（我建议从 src/meta/AmMetaJson 开始）的理解。
4. 在你的复述结尾，请向我发起「第二步：确认阶段」的提问。
5. 在我们后续的全部开发中，你输出的所有代码必须是 Production-ready（生产环境可用）的，绝不允许使用 `// TODO`、`// ...` 或任何省略代码的占位符。如果你觉得代码太长一次输出不完，请告诉我，由我指示你分段输出。

以下是《ArcMeta 开发需求文档》全文：[在此处粘贴完整的《ArcMeta 开发需求文档 (终极版)》全文]
提示词二：触发首个模块开发（当它确认并复述后发送）

使用时机：当 Jules 回复了它的理解，并问你“是否可以开始执行”时，发送这段话。

code
Text
download
content_copy
expand_less
确认，你的理解完全正确。现在请严格进入【第三步：执行阶段】，为我编写第一个模块 `AmMetaJson.h` 和 `AmMetaJson.cpp`。

【开箱即用强制要求】：
1. 完整实现：必须包含文档中提到的所有 JSON 字段（包括 encrypt_iv 的 Base64 编解码）以及完整的三步安全写入流程（.tmp 写入 -> 校验 -> 原子重命名 -> 设置 FILE_ATTRIBUTE_HIDDEN 隐藏属性）。
2. 零外部依赖：只允许使用 C++17 标准库、Qt 原生 JSON 类库（QJsonDocument 等）和 Windows API。路径必须使用 std::wstring。
3. 拒绝半成品：请给出完整的头文件和源文件代码，每一行逻辑都要写死写透，绝对不允许使用任何 `// 此处省略具体实现` 等糊弄手段。
4. 交付格式：代码输出完毕后，请立即执行【第四步：自检与反思】和【第五步：汇报】，在末尾附上你的《自检与反思报告》，逐条核对内存、WinAPI 句柄（HANDLE 是否妥善关闭）、文件操作失败时的异常处理逻辑是否已形成闭环。

请开始输出代码。
提示词三：UI 模块开发专用约束（当开发进行到界面层时发送）

使用时机：当底层数据写完，准备让它写 UI 界面（如 MainWindow 或各 Panel）时发送。

code
Text
download
content_copy
expand_less
我们现在进入 UI 模块的开发。请开始编写[填写面板名称，例如：分类面板 CategoryPanel] 的代码。

【UI 开箱即用强制红线】：
1. 严禁绝对定位：不准使用任何 widget->move() 或 widget->resize() 进行绝对坐标计算，必须且只能使用 QLayout 系列布局管理器。
2. 严守像素与颜色规范：请再次查阅需求文档，代码中必须严格应用文档里指定的控件尺寸（如 24x24px 按钮、18x18px 图标、3px 分隔条宽度）和颜色十六进制值，不得随手编造。
3. 纯净代码：UI 控件必须全部在 C++ 代码中手写实现，不使用 .ui 文件，不要省略任何一个在右键菜单中列出的 QAction 项。
4. 交付格式：输出完整代码后，请附上《自检与反思报告》，明确说明你检查了哪些尺寸、边距和颜色映射，以及信号/槽连接是否完整解耦。

请开始输出代码。
提示词四：纠错与拉回规范（当它犯错、偷懒或代码报错时发送）

使用时机：一旦发现它给了带 //TODO 的代码，或者编译报错，不要帮它改，直接把报错信息扔给它并用这段话骂醒它。

code
Text
download
content_copy
expand_less
停止。你刚刚交付的代码未能达到「开箱即用」的标准，甚至违反了我们的契约。出现了以下问题：[在此处粘贴编译报错信息，或指出它哪里用了 TODO 占位符，或哪里逻辑不符合文档]

请立即退回【第四步：自检与反思阶段】：
1. 深刻反思为何在你之前的自检中遗漏了这个问题？你是否忽视了文档中的哪条红线？
2. 给我一份详细的错误原因分析。
3. 给出完整的修复方案代码，并向我请求确认。未经我回复“确认”，不许跳到下一个模块。

建议：你可以新建一个文本文档，把这四个提示词存起来。每次和 AI 交互时，就用这种**“指令+红线+要自检报告”**的句式推进，它就绝对不敢敷衍你，生成的代码质量会发生质的飞跃。